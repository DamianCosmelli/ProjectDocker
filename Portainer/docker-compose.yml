version: '2'

services:
  portainer:
    image: portainer/portainer
    ports:
      - "19500:9000"
    command: -H unix:///var/run/docker.sock
    volumes:
      - //var/run/docker.sock:/var/run/docker.sock
      - ./../dck_volumes/portainer_data:/data